@page "/EventoAdmin"
@inject ListarEventosDeportivosUseCase ListarEventosUseCase
@inject ServicioSesion UsuarioActual
@inject IServicioAutorizacion ServicioAutorizacion

@rendermode InteractiveServer

@code {
    private List<EventoDeportivo> eventos = new();

    protected override void OnInitialized()
    {
        eventos = ListarEventosUseCase.Ejecutar().ToList();
    }

    private void EditarEvento(int eventoId)
    {
        Console.WriteLine($"Editar evento {eventoId}");
        // Redirigir o mostrar modal, según tu implementación
    }

    private void EliminarEvento(int eventoId)
    {
        Console.WriteLine($"Eliminar evento {eventoId}");
        // Confirmar y eliminar, según tu implementación
    }
}

<h3>Eventos Registrados</h3>

@if (eventos.Count == 0)
{
    <p>No hay eventos registrados.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Fecha y Hora</th>
                <th>Duración (hs)</th>
                <th>Cupo Máximo</th>
                <th>Responsable ID</th>
                @* Columna para acciones *@
                @if ((UsuarioActual.PersonaLogueada?.Id != null) && ServicioAutorizacion.EsAdmin(UsuarioActual.PersonaLogueada!.Id))
                {
                    <th>Acciones</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var evento in eventos)
            {
                <tr>
                    <td>@evento.Id</td>
                    <td>@evento.Nombre</td>
                    <td>@evento.Descripcion</td>
                    <td>@evento.FechaHoraInicio.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>@evento.DuracionHoras</td>
                    <td>@evento.CupoMaximo</td>
                    <td>@evento.ResponsableId</td>
                    @if ((UsuarioActual.PersonaLogueada?.Id != null) && ServicioAutorizacion.EsAdmin(UsuarioActual.PersonaLogueada!.Id))
                    {
                        <td>
                            <button class="btn btn-primary btn-sm me-2" @onclick="() => EditarEvento(evento.Id)">
                                Editar
                            </button>
                            <button class="btn btn-danger btn-sm" @onclick="() => EliminarEvento(evento.Id)">
                                Eliminar
                            </button>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
}
